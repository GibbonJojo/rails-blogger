<h1>Tags</h1>

<table>
    <% @tags.each do |tag| %>
        <tr>
            <td><%= link_to tag.name, tag_path(tag) %> </td>
            <td><%= link_to "Delete Tag", tag_path(tag), method: :delete, data: {confirm: "Do you really want to delete this tag?"}%> </td>
        </tr>
    <% end %>
</table>
<br>
<%= link_to "To Articles", articles_path %>